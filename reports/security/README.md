# Security Reports

This directory tracks privacy-minimized Stage 9 security gate status. Reports
contain pass/fail metadata and scanner versions, not source snippets, secrets,
raw questions, SQL, database rows, credentials, or connection strings.

Regenerate locally with:

```powershell
uv run python scripts/dev.py security-stage9
```
