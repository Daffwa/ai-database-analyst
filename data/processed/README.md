# Processed Runtime Data

`chinook.sqlite` is a byte-identical, verified runtime copy of the pinned raw
artifact. It is generated by `scripts/bootstrap_data.py` and ignored by Git.

The application opens this copy with SQLite URI `mode=ro` and
`PRAGMA query_only = ON`.

