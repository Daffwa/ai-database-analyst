# Raw Chinook Artifact

The expected local files are `Chinook_Sqlite.sqlite` and
`Chinook_PostgreSql.sql` from official release `v1.4.5`. They are not tracked
because both downloads are reproducible and verified before use.

Verification metadata is stored in `SHA256SUMS.txt` and
`../LICENSE-Chinook.md`.
